import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jb.ionic.notes',
  appName: 'JB-NOTES',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
